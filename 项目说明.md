# 校园二手交易平台项目说明

## 一、在线访问网址

在线访问地址：

```text
https://campus-secondhand-db-684z.vercel.app/
```

项目源码仓库：

```text
https://github.com/Island-Zero/campus-secondhand-db
```

## 二、项目简介

本项目实现了一个基于 Flask + SQLite 的“校园二手交易平台”数据库系统。系统围绕校园二手商品交易场景，提供用户信息展示、商品管理、订单管理、购买商品、查询统计、视图展示等功能。

项目主要功能包括：

- 登录权限：区分管理员和普通用户，管理员可以执行写操作，普通用户只能浏览和查询。
- 用户列表：展示平台用户编号、姓名和联系电话。
- 商品管理：展示商品列表，支持新增商品、修改价格、删除未售商品。
- 购买商品：选择未售商品和买家后生成订单，并将商品状态改为已售出。
- 订单列表：展示订单编号、商品名、买家名、订单日期。
- 查询统计：支持基本查询、连接查询、聚合与分组查询、视图查询。
- 安全与并发控制：限制已售商品删除，禁止买家购买自己发布的商品，购买过程使用事务。

## 三、从运行代码到获得最终网址的具体步骤

### 1. 本地运行项目

进入项目目录：

```powershell
cd E:\Code\DB_project
```

安装依赖：

```powershell
python -m pip install -r requirements.txt
```

启动 Flask 项目：

```powershell
python app.py
```

浏览器访问本地地址：

```text
http://127.0.0.1:5000
```

本地运行后可以访问首页、商品页、用户页、订单页、查询页和规则说明页。

### 2. 初始化 Git 并提交代码

项目部署前先将代码提交到 Git：

```powershell
cd E:\Code\DB_project
git init -b main
git add .
git commit -m "Prepare Flask app for deployment"
```

设置 Git 提交用户信息：

```powershell
git config user.name "Island-Zero"
git config user.email "3349392522@qq.com"
```

### 3. 在 GitHub 创建仓库

1. 打开 GitHub：https://github.com
2. 点击右上角 `+`，选择 `New repository`。
3. 仓库名填写：

```text
campus-secondhand-db
```

4. 不勾选 `README`、`.gitignore`、`license`，因为本地项目已经包含这些文件或配置。
5. 点击 `Create repository`。

### 4. 推送项目到 GitHub

添加远程仓库地址：

```powershell
git remote add origin https://github.com/Island-Zero/campus-secondhand-db.git
```

推送到 GitHub：

```powershell
git push -u origin main
```

推送完成后，可以在 GitHub 仓库页面看到项目源码、模板文件、静态文件、数据库脚本和部署配置。

### 5. 使用 Vercel 部署

1. 打开 Vercel：https://vercel.com
2. 使用 GitHub 账号登录。
3. 点击 `Add New Project`。
4. 选择 GitHub 仓库 `Island-Zero/campus-secondhand-db`。
5. 配置如下：

```text
Framework Preset: Flask
Root Directory: ./
Build Command: None
Install Command: pip install -r requirements.txt
```

6. 添加环境变量：

```text
SECRET_KEY = campus-trade-secret-1006
```

7. 点击 `Deploy`。
8. 部署完成后，Vercel 会生成在线访问地址：

```text
https://campus-secondhand-db-684z.vercel.app/
```

### 6. 后续更新部署

如果后续修改代码，只需要执行：

```powershell
git add .
git commit -m "Update project"
git push
```

Vercel 会自动检测 GitHub 仓库的新提交并重新部署。

## 四、数据库设计说明

系统使用 SQLite 数据库文件 `campus_trade.db`，数据库结构也写在 `database.sql` 中。

主要数据表：

- `user(user_id, user_name, phone)`：用户表，保存用户编号、姓名、电话。
- `item(item_id, item_name, category, price, status, seller_id)`：商品表，保存商品名称、分类、价格、状态、卖家编号。
- `orders(order_id, item_id, buyer_id, order_date)`：订单表，保存订单编号、商品编号、买家编号、订单日期。

主要约束：

- `user.user_id`、`item.item_id`、`orders.order_id` 是主键。
- `item.seller_id` 外键引用 `user.user_id`。
- `orders.buyer_id` 外键引用 `user.user_id`。
- `orders.item_id` 外键引用 `item.item_id`。
- `orders.item_id` 设置 `UNIQUE`，保证每个商品最多成交一次。
- `item.status` 使用 `CHECK (status IN (0, 1))`，其中 0 表示未售出，1 表示已售出。

视图：

- `sold_item_view`：已售商品视图，展示已售商品名称和买家 ID。
- `unsold_item_view`：未售商品视图，展示未售商品的编号、名称、分类、价格和卖家 ID。

触发器：

- 限制订单中出现的商品必须是已售状态。
- 限制已有订单的商品不能被改回未售状态。

## 五、系统功能说明

### 1. 首页

首页展示平台概览，包括用户数、商品数、未售商品数、订单数，并提供商品市场、订单记录、用户目录、查询统计等入口。

### 2. 用户管理

用户页面展示 `user` 表中的用户信息，包括：

- 用户编号
- 用户姓名
- 联系电话

### 3. 商品管理

商品页面展示所有商品信息，包括：

- 商品编号
- 商品名称
- 分类
- 价格
- 卖家
- 商品状态

商品管理支持：

- 新增商品：填写商品名、分类、价格和卖家后写入 `item` 表。
- 修改价格：对已有商品执行价格更新。
- 删除未售商品：只允许删除 `status = 0` 的商品。
- 搜索与排序：支持按商品名、分类、卖家搜索，并支持价格排序、未售优先。

### 4. 购买商品

购买商品时，系统会：

1. 检查商品是否存在。
2. 检查商品是否已售出。
3. 检查买家是否为该商品的卖家，禁止购买自己发布的商品。
4. 开启事务。
5. 将商品 `status` 改为 1。
6. 插入 `orders` 订单记录。
7. 提交事务。

如果任一步失败，事务回滚，避免商品状态和订单记录不一致。

### 5. 订单管理

订单页面展示所有订单信息，包括：

- 订单编号
- 商品编号
- 商品名称
- 买家编号
- 买家姓名
- 订单日期

### 6. 查询统计

查询页面支持交互式查询，用户可以输入条件后查看查询结果。查询结果区域使用 AJAX 局部更新，不刷新整个页面。

基本查询：

1. 查询所有未售出的商品。
2. 查询价格大于指定值的商品。
3. 查询指定分类的商品。
4. 查询某个卖家发布的所有商品。

连接查询：

1. 查询所有已售商品及其买家姓名。
2. 查询每个订单：商品名、买家 ID、买家名和日期。
3. 查询卖家是 `u001` 的商品是否被购买。

聚合与分组：

1. 统计商品总数。
2. 统计每类商品数量。
3. 计算商品平均价格。
4. 查询发布商品数量最多的用户。

视图查询：

1. 查询已售商品视图 `sold_item_view`。
2. 查询未售商品视图 `unsold_item_view`。

## 六、网页截图和运行结果截图清单

### 1. 全部网页截图

| 截图名称 | 简要说明 |
| --- | --- |
| `01-首页.png` | 展示在线访问首页、平台概览数据和功能入口。 |
| `02-商品市场.png` | 展示商品卡片列表、商品价格、分类、卖家和状态。 |
| `03-用户列表.png` | 展示所有用户的用户编号、姓名和电话。 |
| `04-订单列表.png` | 展示订单编号、商品名、买家名和订单日期。 |
| `05-查询统计.png` | 展示查询统计页面的查询分类、筛选表单和结果区域。 |
| `06-平台规则.png` | 展示安全性、并发与恢复说明页面。 |
| `07-登录页面.png` | 展示管理员和普通用户登录入口及测试账号说明。 |

### 2. 数据操作任务截图

| 截图名称 | 简要说明 |
| --- | --- |
| `08-普通用户只读模式.png` | 普通用户登录后不显示增删改和购买操作区。 |
| `09-管理员操作区.png` | 管理员登录后显示新增、改价、删除和购买操作。 |
| `10-新增商品前.png` | 新增商品前的商品列表状态。 |
| `11-新增商品后.png` | 提交新增商品后，新商品出现在商品列表中。 |
| `12-修改价格前.png` | 修改商品价格前的原价格。 |
| `13-修改价格后.png` | 修改价格后商品价格发生变化。 |
| `14-删除未售商品前.png` | 删除未售商品前，该商品仍显示在列表中。 |
| `15-删除未售商品后.png` | 删除后商品列表中不再显示该未售商品。 |
| `16-购买商品前.png` | 购买前商品状态为未售出。 |
| `17-购买商品后.png` | 购买后商品状态变为已售出。 |
| `18-购买后订单新增.png` | 购买完成后订单列表新增对应订单记录。 |
| `19-禁止购买自己商品.png` | 选择买家为商品卖家时，系统禁止自买并给出提示。 |

### 3. 基本查询运行结果截图

| 截图名称 | 简要说明 |
| --- | --- |
| `20-查询未售商品.png` | 展示所有 `status = 0` 的未售商品。 |
| `21-按价格筛选商品.png` | 展示价格大于指定值的商品。 |
| `22-按分类查询商品.png` | 展示指定分类下的商品，例如 `DailyGoods`。 |
| `23-查询卖家商品.png` | 展示指定卖家，例如 `u001` 发布的所有商品。 |

### 4. 连接查询运行结果截图

| 截图名称 | 简要说明 |
| --- | --- |
| `24-已售商品及买家.png` | 展示所有已售商品及其买家姓名。 |
| `25-订单商品买家日期.png` | 展示每个订单的商品名、买家 ID、买家名和日期。 |
| `26-卖家商品是否被购买.png` | 展示卖家 `u001` 的商品是否已经被购买。 |

### 5. 聚合与分组运行结果截图

| 截图名称 | 简要说明 |
| --- | --- |
| `27-商品总数统计.png` | 展示商品总数量。 |
| `28-每类商品数量.png` | 展示每个商品分类下的商品数量。 |
| `29-平均价格统计.png` | 展示商品平均价格。 |
| `30-发布最多用户.png` | 展示发布商品数量最多的用户。 |

### 6. 视图查询运行结果截图

| 截图名称 | 简要说明 |
| --- | --- |
| `31-已售商品视图.png` | 展示 `sold_item_view` 查询结果。 |
| `32-未售商品视图.png` | 展示 `unsold_item_view` 查询结果。 |

### 7. 部署结果截图

| 截图名称 | 简要说明 |
| --- | --- |
| `33-GitHub仓库.png` | 展示项目已上传到 GitHub 仓库。 |
| `34-Vercel部署成功.png` | 展示 Vercel 部署状态为 Ready。 |
| `35-在线网址访问.png` | 展示通过在线网址访问项目首页。 |

## 七、运行结果简要说明

系统运行后，用户可以从首页进入商品市场，查看商品列表并执行新增、改价、删除未售商品、购买商品等操作。所有操作都会写入 SQLite 数据库。

系统提供两类登录账号：

- 管理员账号：`admin`，密码：`admin123`。
- 普通用户账号：`user`，密码：`user123`。

普通用户只能浏览页面和查询数据；管理员登录后可以执行新增商品、修改价格、删除未售商品、购买商品和重置数据等写操作。

购买商品后，商品状态从“未售出”变为“已售出”，同时 `orders` 表新增订单记录。订单页面和查询页面可以同步看到数据变化。

查询统计页面可以通过表单输入条件，查看不同 SQL 查询的执行结果。页面保留 SQL 预览，便于说明查询语句和运行结果之间的对应关系。

## 八、安全性

### 1. 如何防止普通用户删除数据

本项目已经增加登录身份和角色权限控制，系统区分管理员和普通用户：

- 管理员账号：`admin`，密码：`admin123`。
- 普通用户账号：`user`，密码：`user123`。

普通用户只能浏览页面和执行查询，不能新增商品、修改价格、删除商品、购买商品或重置数据库。管理员登录后才可以执行这些会修改数据库的操作。

本项目中做了以下限制：

- 页面会根据登录角色控制按钮显示，普通用户页面不显示删除、改价、新增、购买和重置按钮。
- 后端对所有写操作接口增加管理员权限校验，即使普通用户绕过前端直接请求接口，也会被拒绝。
- 后端删除接口仍会检查商品状态，已售商品不能删除。
- 数据库通过外键和订单约束保证订单数据不被破坏。

如果扩展为真实系统，还可以将账号密码保存到数据库中，使用密码哈希存储，并在数据库层面限制普通账号的 `DELETE` 权限。

### 2. 如何限制用户只能查询数据

本项目已经在应用层实现普通用户只读控制：

- 普通用户登录后可以访问首页、商品页、用户页、订单页、查询页和规则页。
- 普通用户可以查看商品、用户、订单和各种查询统计结果。
- 普通用户不能提交新增、修改、删除、购买、重置等写操作。
- 后端通过管理员权限装饰器保护写接口，无管理员权限时直接拒绝。

如果进一步扩展到真实数据库系统，可以为普通用户配置只拥有 `SELECT` 权限的数据库账号。对 SQLite 而言，也可以在查询场景中使用只读方式打开数据库文件，避免查询用户执行写入操作。

## 九、并发与恢复

### 1. 两个用户同时购买同一商品会出现什么问题

如果没有并发控制，两个用户可能同时读取到同一商品的状态为未售出，然后分别提交购买请求。这样可能导致同一商品被重复购买，产生重复订单，造成商品状态和订单数据不一致。

### 2. 如何解决

本项目在购买商品时使用事务控制：

```sql
BEGIN IMMEDIATE
```

购买流程会先获取写锁，然后检查商品状态。如果商品不存在、已经售出，或买家就是卖家本人，则回滚事务并提示失败。

同时，`orders.item_id` 设置了 `UNIQUE` 约束，保证同一商品最多只能出现在一个订单中。即使并发请求同时到达，也只有一个请求能成功生成订单，另一个请求会失败或被回滚。

### 3. 如果系统崩溃，如何恢复订单数据

数据库事务具有原子性。如果系统在购买过程中崩溃：

- 事务未提交：数据库会自动回滚，订单不会写入，商品状态也不会改变。
- 事务已提交：订单记录和商品状态会保留。

为了进一步保证数据安全，实际部署中应定期备份数据库文件。生产环境还可以使用 MySQL、PostgreSQL 等数据库，通过 WAL、binlog、自动备份和恢复机制恢复订单数据。
