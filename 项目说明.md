# 校园二手交易平台项目说明

## 一、在线访问网址

在线访问地址：

```text
https://campus-secondhand-db-684z.vercel.app/
```

项目源码仓库：

```text
https://github.com/Island-Zero/campus-secondhand-db
```

## 二、项目概述

校园二手交易平台是一个基于 Flask + SQLite 的数据库课程项目。系统围绕校园闲置物品交易场景设计，提供用户管理、商品管理、订单管理、商品购买、查询统计、视图查询、事务处理和权限控制等功能。

系统采用 B/S 架构。前端页面使用 HTML、CSS 和 Jinja2 模板实现，后端使用 Flask 处理路由、表单提交和数据库操作，数据库使用 SQLite 保存用户、商品和订单数据。项目代码托管在 GitHub，并通过 Vercel 部署为可在线访问网页。

系统主要功能包括：

- 登录与权限控制：区分管理员和普通用户。管理员可以执行新增、修改、删除、购买、重置等写操作；普通用户只能浏览和查询。
- 用户管理：展示用户编号、用户名和联系电话。
- 商品管理：展示商品信息，支持新增商品、修改商品价格、删除未售商品。
- 订单管理：展示订单编号、商品名称、买家信息和订单日期。
- 购买商品：购买后在订单表中新增记录，并将商品状态修改为已售出。
- 查询统计：实现基本查询、连接查询、聚合分组查询和视图查询。
- 安全与一致性控制：限制普通用户写操作，禁止购买自己发布的商品，购买流程使用事务保证数据一致性。

## 三、从运行代码到获得最终网址的步骤

### 1. 本地运行项目

进入项目目录：

```powershell
cd E:\Code\DB_project
```

安装依赖：

```powershell
python -m pip install -r requirements.txt
```

启动 Flask 服务：

```powershell
python app.py
```

浏览器访问：

```text
http://127.0.0.1:5000
```

本地运行后，可以访问首页、登录页、商品页、用户页、订单页、查询页和规则说明页。

### 2. 初始化 Git 仓库

```powershell
cd E:\Code\DB_project
git init -b main
git add .
git commit -m "Prepare Flask app for deployment"
```

设置 Git 用户信息：

```powershell
git config user.name "Island-Zero"
git config user.email "3349392522@qq.com"
```

### 3. 创建 GitHub 仓库

1. 打开 GitHub：https://github.com
2. 点击右上角 `+`，选择 `New repository`。
3. 仓库名称填写：

```text
campus-secondhand-db
```

4. 不勾选 `README`、`.gitignore` 和 `license`，因为本地项目已经包含相关文件。
5. 点击 `Create repository`。

### 4. 推送代码到 GitHub

添加远程仓库：

```powershell
git remote add origin https://github.com/Island-Zero/campus-secondhand-db.git
```

推送代码：

```powershell
git push -u origin main
```

推送完成后，GitHub 仓库中可以查看项目源码、模板文件、静态样式文件、数据库脚本和部署配置。

### 5. 使用 Vercel 部署

1. 打开 Vercel：https://vercel.com
2. 使用 GitHub 账号登录。
3. 点击 `Add New Project`。
4. 选择 GitHub 仓库 `Island-Zero/campus-secondhand-db`。
5. 部署配置如下：

```text
Framework Preset: Flask
Root Directory: ./
Build Command: None
Install Command: pip install -r requirements.txt
```

6. 添加环境变量：

```text
SECRET_KEY = campus-trade-secret-1006
```

7. 点击 `Deploy`。
8. 部署状态为 `Ready` 后，获得最终在线访问网址：

```text
https://campus-secondhand-db-684z.vercel.app/
```

### 6. 后续更新方式

后续修改代码后执行：

```powershell
git add .
git commit -m "Update project"
git push
```

Vercel 会自动检测 GitHub 仓库 `main` 分支的新提交并重新部署。

说明：系统使用 SQLite 数据库，适合课程项目和在线演示。Vercel 的运行环境不适合作为长期持久化数据库环境，因此重新部署或运行环境重置后，线上数据可能恢复到初始状态。若用于真实生产环境，建议迁移到 MySQL、PostgreSQL 等持久化数据库。

## 四、数据库设计

系统数据库文件为 `data/campus_trade.db`，数据库结构定义保存在 `data/database.sql` 中。数据库主要包含三张表：`user`、`item`、`orders`。

### 1. user 表

`user` 表保存平台用户信息。

- `user_id`：用户编号，主键。
- `user_name`：用户姓名。
- `phone`：联系电话，唯一。

### 2. item 表

`item` 表保存商品信息。

- `item_id`：商品编号，主键。
- `item_name`：商品名称。
- `category`：商品分类。
- `price`：商品价格。
- `status`：商品状态，0 表示未售出，1 表示已售出。
- `seller_id`：卖家编号，外键引用 `user.user_id`。

### 3. orders 表

`orders` 表保存交易订单信息。

- `order_id`：订单编号，主键。
- `item_id`：商品编号，外键引用 `item.item_id`。
- `buyer_id`：买家编号，外键引用 `user.user_id`。
- `order_date`：订单日期。

### 4. 完整性约束

- `user.user_id`、`item.item_id`、`orders.order_id` 为主键。
- `item.seller_id` 外键引用 `user.user_id`。
- `orders.buyer_id` 外键引用 `user.user_id`。
- `orders.item_id` 外键引用 `item.item_id`。
- `orders.item_id` 设置 `UNIQUE`，保证同一商品最多成交一次。
- `item.status` 使用 `CHECK (status IN (0, 1))` 限制商品状态取值。

### 5. 视图

系统创建两个视图：

- `sold_item_view`：已售商品视图，包含商品名和买家 ID。
- `unsold_item_view`：未售商品视图，包含未售商品编号、名称、分类、价格和卖家 ID。

### 6. 触发器

系统通过触发器保证订单和商品状态一致：

- 插入订单时，订单中的商品必须处于已售状态。
- 已有订单的商品不能被改回未售状态。

## 五、系统功能说明

### 1. 登录与权限控制

系统使用登录身份和角色权限控制。账号分为管理员和普通用户：

- 管理员账号：`admin`，密码：`admin123`。
- 普通用户账号：`user`，密码：`user123`。

管理员可以执行新增商品、修改价格、删除未售商品、购买商品和重置数据等写操作。普通用户可以浏览首页、商品页、用户页、订单页、查询页和规则页，但不能执行修改数据库的操作。

### 2. 首页

首页展示平台概览数据，包括用户数量、商品数量、未售商品数量和订单数量，并提供商品市场、订单记录、用户目录和查询统计等功能入口。

### 3. 用户管理

用户页面展示 `user` 表中的全部用户信息，包括用户编号、用户名和联系电话。

### 4. 商品管理

商品页面展示商品编号、商品名称、分类、价格、卖家和商品状态。管理员登录后可以新增商品、修改价格和删除未售商品。普通用户访问商品页面时只能查看商品信息。

商品页面还支持前端搜索和排序，可以按商品名、分类、卖家搜索，也可以按价格和未售状态排序。

### 5. 购买商品

管理员可以在商品页面选择未售商品和买家生成订单。购买流程包括：

1. 检查商品是否存在。
2. 检查商品是否已售出。
3. 检查买家是否为该商品卖家，禁止购买自己发布的商品。
4. 开启数据库事务。
5. 将商品状态改为已售出。
6. 插入订单记录。
7. 提交事务。

如果任一步失败，事务回滚，避免商品状态和订单记录不一致。

### 6. 订单管理

订单页面展示全部订单记录，包括订单编号、商品编号、商品名称、买家编号、买家姓名和订单日期。

### 7. 查询统计

查询页面支持交互式查询，用户可以选择查询任务并输入筛选条件。查询结果区域使用 AJAX 局部更新，不需要刷新整个页面。

基本查询包括：

1. 查询所有未售出的商品。
2. 查询价格大于指定值的商品。
3. 查询指定分类的商品。
4. 查询某个卖家发布的所有商品。

连接查询包括：

1. 查询所有已售商品及其买家姓名。
2. 查询每个订单的商品名、买家 ID、买家名和日期。
3. 查询卖家是 `u001` 的商品是否被购买。

聚合与分组查询包括：

1. 统计商品总数。
2. 统计每类商品数量。
3. 计算商品平均价格。
4. 查询发布商品数量最多的用户。

视图查询包括：

1. 查询已售商品视图 `sold_item_view`。
2. 查询未售商品视图 `unsold_item_view`。

## 六、网页截图和运行结果截图说明

### 1. 全部网页截图

首页
![首页](alt_md_images/image-5.png)
系统首页，包括顶部导航栏、平台数据概览、当前用户数、商品数、未售商品数和订单数，用户可以通过首页入口访问商品市场、订单记录、用户目录和查询统计页面。

商品市场
![商品市场](alt_md_images/image-1.png)
商品市场页面的商品卡片列表，每个商品显示商品名、价格、分类、卖家、商品编号和售出状态，对应数据库中的 `item` 表和 `user` 表关联结果。

商品市场操作区
![商品市场操作区](alt_md_images/image-2.png)
管理员视角下的商品管理区域，包括新增商品表单和购买商品表单，表单提交后会分别执行 `INSERT INTO item`、`UPDATE item` 和 `INSERT INTO orders` 等数据库操作。

用户列表
![用户列表](alt_md_images/image-3.png)
用户列表页面，包括用户编号、用户名和联系电话，页面数据来自 `user` 表，用于核对商品卖家和订单买家的基础信息。

订单列表
![订单列表](alt_md_images/image-4.png)
订单列表页面，包括订单编号、商品编号、商品名称、买家编号、买家姓名和订单日期，页面通过 `orders`、`item`、`user` 三张表连接查询得到。

### 2. 数据操作

新增商品、修改价格前
![新增商品、修改价格前](alt_md_images/image-12.png)
执行新增商品和修改商品价格前的商品列表状态，用于和后续操作结果进行对比，验证操作前数据库中商品记录和价格字段的原始状态。

新增商品、修改价格后
![新增商品、修改价格后](alt_md_images/image-13.png)
管理员提交新增商品后，新商品出现在商品列表中；修改商品价格后，刷新页面可以看到价格字段变化，说明 `item` 表中的插入和更新操作已经真实生效。

删除未售商品后
![删除未售商品后](alt_md_images/image-14.png)
删除操作完成后，目标商品不再出现在商品列表中，说明后端执行了 `DELETE FROM item`，并且页面刷新后能够看到数据库变化。

### 3. 基本查询

查询所有未售商品
![查询所有未售商品](alt_md_images/image-15.png)
所有 `status = 0` 的未售商品查询结果，查询结果来自 `item` 表或未售商品视图，用于验证未售商品筛选功能。

查询价格大于 30 的商品
![查询价格大于 30 的商品](alt_md_images/image-16.png)
价格大于 30 的商品查询结果，对应 SQL 条件 `price > 30`，用于验证按价格筛选商品的基本查询功能。

查询生活用品类商品
![查询生活用品类商品](alt_md_images/image-17.png)
“生活用品”类商品查询结果，本系统中生活用品分类对应 `DailyGoods`，查询条件作用于 `item.category` 字段。

查询 u001 发布的商品
![查询 u001 发布的商品](alt_md_images/image-18.png)
卖家编号为 `u001` 的所有商品，对应 SQL 条件 `seller_id = 'u001'`，用于验证按卖家筛选商品功能。

### 4. 连接查询

已售商品及买家
![已售商品及买家](alt_md_images/image-19.png)
所有已售商品及其买家姓名，查询通过连接 `item`、`orders` 和 `user` 表得到商品名、买家编号和买家姓名。

订单明细
![订单明细](alt_md_images/image-20.png)
每个订单的商品名、买家 ID、买家名和订单日期，查询通过 `orders` 表连接 `item` 表和 `user` 表实现，用于说明订单和商品、用户之间的关联关系。

卖家商品购买状态
![卖家商品购买状态](alt_md_images/image-21.png)
卖家 `u001` 发布的商品是否已经被购买，查询通过商品表左连接订单表实现，能够区分已购买和未购买商品。

### 5. 聚合统计

商品总数
![商品总数](alt_md_images/image-22.png)
商品总数量统计结果，对应 SQL 聚合函数 `COUNT(*)`，用于统计 `item` 表中的商品记录总数。

每类商品数量
![每类商品数量](alt_md_images/image-23.png)
每个商品分类下的商品数量，对应 `GROUP BY category` 和 `COUNT(*)`，用于验证分组统计功能。

平均价格
![平均价格](alt_md_images/image-24.png)
所有商品的平均价格，对应 SQL 聚合函数 `AVG(price)`，用于统计商品价格的整体水平。

发布最多用户
![发布最多用户](alt_md_images/image-25.png)
发布商品数量最多的用户，通过连接 `user` 表和 `item` 表并按用户分组统计商品数量得到。

### 6. 视图

已售商品视图
![已售商品视图](alt_md_images/image-26.png)
`sold_item_view` 的查询结果，该视图包含已售商品名和买家 ID，用于简化已售商品查询。

未售商品视图
![未售商品视图](alt_md_images/image-27.png)
`unsold_item_view` 的查询结果，该视图包含未售商品编号、商品名、分类、价格和卖家 ID，用于快速查看当前仍可购买的商品。

### 7. 业务逻辑实现

购买商品前
![购买商品前](alt_md_images/image-28.png)
购买前目标商品状态为未售出，可以在购买表单中选择该商品和买家，后端会在提交前检查商品状态、买家身份和订单唯一性。

购买商品后
![购买商品后](alt_md_images/image-29.png)
购买完成后目标商品状态变为已售出，说明 `item.status` 已由 0 更新为 1，商品不能再作为未售商品继续出售。

订单新增
![订单新增](alt_md_images/image-30.png)
购买完成后订单列表新增对应订单记录，说明系统在 `orders` 表中插入了新的订单数据。

已售商品不能重复购买
![已售商品不能重复购买](alt_md_images/image-31.png)
已售商品无法选择再次购买，系统通过商品状态检查和 `orders.item_id` 唯一约束防止同一商品重复成交。

禁止购买自己商品
![禁止购买自己商品](alt_md_images/image-32.png)
买家无法选择为商品卖家本人，说明购买逻辑中包含卖家和买家不能相同的业务校验。

### 8. 用户权限

普通用户只读
![普通用户只读](alt_md_images/image-10.png)
普通用户登录后的页面状态，普通用户可以查看商品、用户、订单和查询结果，但不能看到或提交新增、修改、删除、购买和重置操作。

管理员操作区
![管理员操作区](alt_md_images/image-11.png)
管理员登录后的页面状态，管理员可以执行商品新增、价格修改、删除未售商品、购买商品和重置数据等写操作。

### 9. 系统部署

GitHub 仓库
![GitHub 仓库](alt_md_images/image-33.png)
GitHub 仓库页面，说明项目源码、模板文件、静态资源、数据库脚本和部署配置已经上传到远程仓库。

Vercel 部署成功
![Vercel 部署成功](alt_md_images/image-34.png)
Vercel 项目部署状态为 `Ready`，说明 GitHub 仓库已经成功部署为在线 Web 服务。

在线访问网址
![在线访问网址](alt_md_images/image.png)
通过 `https://campus-secondhand-db-684z.vercel.app/` 访问系统首页，项目已经获得可在线访问的最终网址。


## 七、运行结果说明

系统运行后，用户可以通过在线网址进入首页，并根据导航进入商品、用户、订单、查询和规则页面。管理员登录后可以执行新增商品、修改价格、删除未售商品和购买商品等操作。普通用户登录后只能查看数据和执行查询。

新增商品操作会向 `item` 表插入记录；修改价格操作会更新 `item.price`；删除操作只允许删除未售商品；购买操作会在事务中更新商品状态并向 `orders` 表插入订单记录。

查询统计页面可以显示不同 SQL 查询的执行结果，包括基本查询、连接查询、聚合分组查询和视图查询。查询结果与 SQL 预览同时展示，便于说明查询语句和结果之间的对应关系。

## 八、安全性

### 1. 如何防止普通用户删除数据

系统使用登录身份和角色权限控制区分管理员和普通用户：

- 管理员账号：`admin`，密码：`admin123`。
- 普通用户账号：`user`，密码：`user123`。

普通用户只能浏览页面和执行查询，不能新增商品、修改价格、删除商品、购买商品或重置数据库。管理员登录后可以执行这些会修改数据库的操作。

系统在前端和后端同时进行权限控制：

- 前端根据登录角色控制按钮显示，普通用户页面不显示删除、改价、新增、购买和重置按钮。
- 后端对所有写操作接口增加管理员权限校验，即使普通用户绕过前端直接请求接口，也会被拒绝。
- 删除接口会检查商品状态，已售商品不能删除。
- 数据库通过外键和订单约束保证订单数据不会被破坏。

如果扩展为真实系统，可以将账号信息保存到数据库中，使用密码哈希存储，并在数据库层面限制普通账号的 `DELETE` 权限。

### 2. 如何限制用户只能查询数据

系统在应用层实现普通用户只读控制：

- 普通用户可以访问首页、商品页、用户页、订单页、查询页和规则页。
- 普通用户可以查看商品、用户、订单和各类查询统计结果。
- 普通用户不能提交新增、修改、删除、购买、重置等写操作。
- 后端通过管理员权限装饰器保护写接口，无管理员权限时直接拒绝请求。

如果扩展到真实数据库系统，可以为普通用户配置只拥有 `SELECT` 权限的数据库账号。对 SQLite 而言，也可以在查询场景中使用只读方式打开数据库文件，避免查询用户执行写入操作。

## 九、并发与恢复

### 1. 两个用户同时购买同一商品会出现什么问题

如果没有并发控制，两个用户可能同时读取到同一商品的状态为未售出，然后分别提交购买请求。这会导致同一商品被重复购买，产生重复订单，造成商品状态和订单数据不一致。

### 2. 如何解决

系统在购买商品时使用数据库事务控制：

```sql
BEGIN IMMEDIATE
```

购买流程会先获取写锁，然后检查商品状态。如果商品不存在、已经售出，或买家就是卖家本人，则回滚事务并提示失败。

同时，`orders.item_id` 设置 `UNIQUE` 约束，保证同一商品最多只能出现在一个订单中。即使并发请求同时到达，也只有一个请求能够成功生成订单，另一个请求会失败或被回滚。

### 3. 如果系统崩溃，如何恢复订单数据

数据库事务具有原子性。如果系统在购买过程中崩溃：

- 事务未提交：数据库会自动回滚，订单不会写入，商品状态也不会改变。
- 事务已提交：订单记录和商品状态会保留。

为了进一步保证数据安全，实际部署中应定期备份数据库文件。生产环境也可以使用 MySQL、PostgreSQL 等数据库，通过 WAL、binlog、自动备份和恢复机制恢复订单数据。
