# 在线访问网址

部署完成后填写 Render 生成的网址，例如：

https://campus-secondhand-db.onrender.com

# 项目简介

本项目实现“校园二手交易平台数据库系统”，使用 Flask + SQLite。网页包含首页、商品列表、用户列表、订单列表、查询与统计页面。所有增删改查都真实作用于 SQLite 数据库，刷新页面后可以看到数据变化。

# 本地运行步骤

1. 进入项目目录：

```powershell
cd E:\Code\DB_project
```

2. 安装依赖：

```powershell
python -m pip install -r requirements.txt
```

3. 启动项目：

```powershell
python app.py
```

4. 浏览器访问：

```text
http://127.0.0.1:5000
```

# 获得在线网址的步骤

1. 将本项目上传到 GitHub 仓库。
2. 打开 https://render.com，新建 Web Service。
3. 连接 GitHub 仓库。
4. Render 会读取 `render.yaml`，自动使用：

```text
buildCommand: pip install -r requirements.txt
startCommand: gunicorn app:app
```

5. 部署完成后复制 Render 提供的公网 URL，填写到本文档开头。

# 数据库定义

项目启动时会自动创建 `campus_trade.db`，包含三张表：

- `user(user_id, user_name, phone)`
- `item(item_id, item_name, category, price, status, seller_id)`
- `orders(order_id, item_id, buyer_id, order_date)`

完整 SQL 见 `database.sql`。程序中的建表和初始数据写在 `app.py` 的 `init_db()` 中。

主要完整性约束：

- `user.user_id`、`item.item_id`、`orders.order_id` 是主键。
- `orders.item_id` 设置 `UNIQUE`，保证每个商品最多只能被交易一次。
- `item.status` 使用 `CHECK (status IN (0, 1))`，0 表示未售出，1 表示已售出。
- `item.seller_id` 外键引用 `user.user_id`。
- `orders.buyer_id` 外键引用 `user.user_id`。
- `orders.item_id` 外键引用 `item.item_id`。
- 触发器保证出现在 `orders` 表中的商品 `status` 必须为 1，已有订单的商品不能改回 0。

# 数据操作说明

在“商品列表”页面可以完成：

- 插入一个新商品：填写商品名、类别、价格、卖家，提交后写入 `item` 表。
- 修改商品价格：在商品行内输入新价格并提交，执行 `UPDATE item SET price = ?`。
- 删除未售商品：只对 `status = 0` 的商品显示删除按钮，执行 `DELETE FROM item`。
- 购买商品：选择未售商品和买家，程序在事务中先将商品 `status` 改为 1，再插入 `orders` 记录。

# 基本查询

在“查询与统计”页面展示：

1. 查询所有未售出的商品。
2. 查询价格大于 30 的商品。
3. 查询“生活用品”类商品，本项目按初始数据字段展示为 `DailyGoods`。
4. 查询 `u001` 发布的所有商品。

# 连接查询

在“查询与统计”页面展示：

1. 所有已售商品及其买家姓名。
2. 每个订单的商品名、买家名、日期。
3. 卖家是 `u001` 的商品是否被购买。

# 聚合与分组

在“查询与统计”页面展示：

1. 商品总数。
2. 每类商品数量。
3. 所有商品平均价格。
4. 发布商品数量最多的用户。

# 视图

项目创建两个视图：

- `sold_item_view`：已售商品视图，包含商品名和买家 ID。
- `unsold_item_view`：未售商品视图。

两个视图的内容在“查询与统计”页面展示。

# 业务逻辑：购买商品

购买商品使用 SQL 事务实现：

1. `BEGIN IMMEDIATE` 获取写锁，避免两个请求同时修改。
2. 查询商品状态。
3. 如果商品不存在或 `status = 1`，回滚并提示失败。
4. 如果商品未售出，执行 `UPDATE item SET status = 1`。
5. 插入 `orders` 表。
6. `COMMIT` 提交。

这样可以保证订单新增和商品状态修改同时成功或同时失败，并阻止已售商品再次购买。

# 安全性说明

1. 防止普通用户删除数据：实际系统中应区分普通用户和管理员。普通用户账号不授予 `DELETE` 权限，后端接口也要做登录身份和角色校验；只有管理员角色可以访问删除接口。本项目演示页面只允许删除未售出商品，且数据库外键和触发器会阻止破坏订单一致性的删除。
2. 限制用户只能查询数据：数据库层可以创建只读账号，只授予 `SELECT` 权限；应用层可以只开放查询路由，不开放增删改接口。对 SQLite 部署时，也可以把查询端使用只读连接模式打开数据库文件。

# 并发与恢复说明

1. 两个用户同时购买同一商品时，如果没有控制，可能两个请求都读取到 `status = 0`，然后分别插入订单，造成同一商品被交易两次。
2. 解决方式：使用事务和锁。项目中的购买逻辑使用 `BEGIN IMMEDIATE` 获取写锁，同时 `orders.item_id` 有唯一约束，因此第二个购买请求会失败，不会生成重复订单。
3. 系统崩溃后的恢复：数据库使用事务日志保证原子性。如果购买过程中崩溃，未提交事务会回滚；已提交事务会保留。生产环境还应定期备份数据库文件，或使用 MySQL/PostgreSQL 的 WAL、binlog、备份恢复机制恢复订单数据。

# 截图清单

提交前请把实际运行截图放入说明文档或压缩包中，建议包含：

- 首页截图。
- 商品列表截图。
- 插入新商品前后截图。
- 修改价格前后截图。
- 删除未售商品前后截图。
- 购买商品后订单新增、商品状态变为已售出的截图。
- 用户列表截图。
- 订单列表截图。
- 查询与统计页面所有查询结果截图。

# 视频录制清单

视频建议按以下顺序操作：

1. 输入在线网址进入首页。
2. 进入商品列表、用户列表、订单列表。
3. 插入一个新商品并刷新验证。
4. 修改一个商品价格并刷新验证。
5. 删除一个未售商品并刷新验证。
6. 购买一个未售商品，展示订单新增和商品状态变化。
7. 进入查询与统计页面，展示基本查询、连接查询、聚合分组和视图。
